.env
.env.local
.env.*.local
.env.staging
.staging/
node_modules
dist
.next
.turbo
logs
coverage
*.log
next-env.d.ts
tsconfig.tsbuildinfo

# Secrets / credentials — never commit
*.json.key
gcp-vision.json
makyn-*.json
ca-certificate.crt
*.pem
*.key

/.claude/worktrees/
.DS_Store

# Env backups (from deploy.sh pre-write snapshots)
.env.bak*
.env.*.backup
.env.backup*

# Local backup dir (deploy snapshots)
/backups/
